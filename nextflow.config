docker {
    enabled         = true
    autoMounts      = true
    runOptions      = '-u $(id -u):$(id -g)' 
    fixOwnership    = true
    remove          = true
}

process {
    executor = 'local'
}

manifest.name = 'playground'
manifest.description = 'playground'
manifest.version = null